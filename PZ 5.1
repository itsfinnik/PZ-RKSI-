def sum_from_n_to_m(n, m):
    return sum(range(n, m + 1))


n = int(input("Введите n: "))
m = int(input("Введите m: "))

# если пользователь ввёл n больше m — меняем местами
if n > m:
    n, m = m, n

result = sum_from_n_to_m(n, m)
print("Сумма чисел от", n, "до", m, "равна:", result)
